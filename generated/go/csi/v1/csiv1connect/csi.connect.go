// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: csi/v1/csi.proto

package csiv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/skua-international/magpie/generated/go/csi/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// CapacityServiceName is the fully-qualified name of the CapacityService service.
	CapacityServiceName = "csi.v1.CapacityService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// CapacityServiceReserveCapacityProcedure is the fully-qualified name of the CapacityService's
	// ReserveCapacity RPC.
	CapacityServiceReserveCapacityProcedure = "/csi.v1.CapacityService/ReserveCapacity"
	// CapacityServiceReleaseCapacityProcedure is the fully-qualified name of the CapacityService's
	// ReleaseCapacity RPC.
	CapacityServiceReleaseCapacityProcedure = "/csi.v1.CapacityService/ReleaseCapacity"
)

// CapacityServiceClient is a client for the csi.v1.CapacityService service.
type CapacityServiceClient interface {
	// Ask for `bytes` of headroom to be available before a batch of
	// downloads starts, and to stay available for `ttl_seconds`.
	//
	// Blocks until the blob has actually been resized (growing is a cheap
	// online btrfs resize, so this is fast), so a caller that awaits this
	// before dispatching genuinely has the room it asked for.
	//
	// Reserving the same `key` again replaces that key's previous
	// reservation rather than adding to it, so a retried or re-resolved
	// batch doesn't double-count. Different keys sum.
	ReserveCapacity(context.Context, *connect.Request[v1.ReserveCapacityRequest]) (*connect.Response[v1.ReserveCapacityResponse], error)
	// Drop a reservation early, once the batch it covered has finished.
	// Purely an optimization -- every reservation expires on its own TTL
	// regardless, which is what keeps a crashed or wedged caller from
	// pinning the blob at its high-water mark forever. Releasing an
	// unknown or already-expired key is not an error.
	ReleaseCapacity(context.Context, *connect.Request[v1.ReleaseCapacityRequest]) (*connect.Response[v1.ReleaseCapacityResponse], error)
}

// NewCapacityServiceClient constructs a client for the csi.v1.CapacityService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCapacityServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CapacityServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	capacityServiceMethods := v1.File_csi_v1_csi_proto.Services().ByName("CapacityService").Methods()
	return &capacityServiceClient{
		reserveCapacity: connect.NewClient[v1.ReserveCapacityRequest, v1.ReserveCapacityResponse](
			httpClient,
			baseURL+CapacityServiceReserveCapacityProcedure,
			connect.WithSchema(capacityServiceMethods.ByName("ReserveCapacity")),
			connect.WithClientOptions(opts...),
		),
		releaseCapacity: connect.NewClient[v1.ReleaseCapacityRequest, v1.ReleaseCapacityResponse](
			httpClient,
			baseURL+CapacityServiceReleaseCapacityProcedure,
			connect.WithSchema(capacityServiceMethods.ByName("ReleaseCapacity")),
			connect.WithClientOptions(opts...),
		),
	}
}

// capacityServiceClient implements CapacityServiceClient.
type capacityServiceClient struct {
	reserveCapacity *connect.Client[v1.ReserveCapacityRequest, v1.ReserveCapacityResponse]
	releaseCapacity *connect.Client[v1.ReleaseCapacityRequest, v1.ReleaseCapacityResponse]
}

// ReserveCapacity calls csi.v1.CapacityService.ReserveCapacity.
func (c *capacityServiceClient) ReserveCapacity(ctx context.Context, req *connect.Request[v1.ReserveCapacityRequest]) (*connect.Response[v1.ReserveCapacityResponse], error) {
	return c.reserveCapacity.CallUnary(ctx, req)
}

// ReleaseCapacity calls csi.v1.CapacityService.ReleaseCapacity.
func (c *capacityServiceClient) ReleaseCapacity(ctx context.Context, req *connect.Request[v1.ReleaseCapacityRequest]) (*connect.Response[v1.ReleaseCapacityResponse], error) {
	return c.releaseCapacity.CallUnary(ctx, req)
}

// CapacityServiceHandler is an implementation of the csi.v1.CapacityService service.
type CapacityServiceHandler interface {
	// Ask for `bytes` of headroom to be available before a batch of
	// downloads starts, and to stay available for `ttl_seconds`.
	//
	// Blocks until the blob has actually been resized (growing is a cheap
	// online btrfs resize, so this is fast), so a caller that awaits this
	// before dispatching genuinely has the room it asked for.
	//
	// Reserving the same `key` again replaces that key's previous
	// reservation rather than adding to it, so a retried or re-resolved
	// batch doesn't double-count. Different keys sum.
	ReserveCapacity(context.Context, *connect.Request[v1.ReserveCapacityRequest]) (*connect.Response[v1.ReserveCapacityResponse], error)
	// Drop a reservation early, once the batch it covered has finished.
	// Purely an optimization -- every reservation expires on its own TTL
	// regardless, which is what keeps a crashed or wedged caller from
	// pinning the blob at its high-water mark forever. Releasing an
	// unknown or already-expired key is not an error.
	ReleaseCapacity(context.Context, *connect.Request[v1.ReleaseCapacityRequest]) (*connect.Response[v1.ReleaseCapacityResponse], error)
}

// NewCapacityServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCapacityServiceHandler(svc CapacityServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	capacityServiceMethods := v1.File_csi_v1_csi_proto.Services().ByName("CapacityService").Methods()
	capacityServiceReserveCapacityHandler := connect.NewUnaryHandler(
		CapacityServiceReserveCapacityProcedure,
		svc.ReserveCapacity,
		connect.WithSchema(capacityServiceMethods.ByName("ReserveCapacity")),
		connect.WithHandlerOptions(opts...),
	)
	capacityServiceReleaseCapacityHandler := connect.NewUnaryHandler(
		CapacityServiceReleaseCapacityProcedure,
		svc.ReleaseCapacity,
		connect.WithSchema(capacityServiceMethods.ByName("ReleaseCapacity")),
		connect.WithHandlerOptions(opts...),
	)
	return "/csi.v1.CapacityService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CapacityServiceReserveCapacityProcedure:
			capacityServiceReserveCapacityHandler.ServeHTTP(w, r)
		case CapacityServiceReleaseCapacityProcedure:
			capacityServiceReleaseCapacityHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCapacityServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedCapacityServiceHandler struct{}

func (UnimplementedCapacityServiceHandler) ReserveCapacity(context.Context, *connect.Request[v1.ReserveCapacityRequest]) (*connect.Response[v1.ReserveCapacityResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("csi.v1.CapacityService.ReserveCapacity is not implemented"))
}

func (UnimplementedCapacityServiceHandler) ReleaseCapacity(context.Context, *connect.Request[v1.ReleaseCapacityRequest]) (*connect.Response[v1.ReleaseCapacityResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("csi.v1.CapacityService.ReleaseCapacity is not implemented"))
}
