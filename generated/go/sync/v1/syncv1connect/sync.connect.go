// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: sync/v1/sync.proto

package syncv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/skua-international/magpie/generated/go/sync/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SyncServiceName is the fully-qualified name of the SyncService service.
	SyncServiceName = "sync.v1.SyncService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SyncServiceRegisterSourceProcedure is the fully-qualified name of the SyncService's
	// RegisterSource RPC.
	SyncServiceRegisterSourceProcedure = "/sync.v1.SyncService/RegisterSource"
	// SyncServiceDeregisterSourceProcedure is the fully-qualified name of the SyncService's
	// DeregisterSource RPC.
	SyncServiceDeregisterSourceProcedure = "/sync.v1.SyncService/DeregisterSource"
	// SyncServiceSyncContentProcedure is the fully-qualified name of the SyncService's SyncContent RPC.
	SyncServiceSyncContentProcedure = "/sync.v1.SyncService/SyncContent"
	// SyncServiceGetSourceModsProcedure is the fully-qualified name of the SyncService's GetSourceMods
	// RPC.
	SyncServiceGetSourceModsProcedure = "/sync.v1.SyncService/GetSourceMods"
	// SyncServiceRefreshSourceProcedure is the fully-qualified name of the SyncService's RefreshSource
	// RPC.
	SyncServiceRefreshSourceProcedure = "/sync.v1.SyncService/RefreshSource"
	// SyncServiceListSyncedModsProcedure is the fully-qualified name of the SyncService's
	// ListSyncedMods RPC.
	SyncServiceListSyncedModsProcedure = "/sync.v1.SyncService/ListSyncedMods"
	// SyncServiceInvalidateModProcedure is the fully-qualified name of the SyncService's InvalidateMod
	// RPC.
	SyncServiceInvalidateModProcedure = "/sync.v1.SyncService/InvalidateMod"
	// SyncServiceGetSyncedModProcedure is the fully-qualified name of the SyncService's GetSyncedMod
	// RPC.
	SyncServiceGetSyncedModProcedure = "/sync.v1.SyncService/GetSyncedMod"
	// SyncServiceGetSyncStatsProcedure is the fully-qualified name of the SyncService's GetSyncStats
	// RPC.
	SyncServiceGetSyncStatsProcedure = "/sync.v1.SyncService/GetSyncStats"
	// SyncServiceRefreshSteamAuthProcedure is the fully-qualified name of the SyncService's
	// RefreshSteamAuth RPC.
	SyncServiceRefreshSteamAuthProcedure = "/sync.v1.SyncService/RefreshSteamAuth"
	// SyncServiceGetSyncStatusProcedure is the fully-qualified name of the SyncService's GetSyncStatus
	// RPC.
	SyncServiceGetSyncStatusProcedure = "/sync.v1.SyncService/GetSyncStatus"
)

// SyncServiceClient is a client for the sync.v1.SyncService service.
type SyncServiceClient interface {
	// Resolve `candidate_ids` (each may be an individual mod or a collection
	// -- the caller doesn't need to know which) and register them as a named
	// source in the persistent sync registry. Registering an existing
	// source_id again refreshes its membership (diffed against what was
	// previously registered) rather than erroring -- this is also how a
	// source is first created, there is no separate "create" call.
	RegisterSource(context.Context, *connect.Request[v1.RegisterSourceRequest]) (*connect.Response[v1.RegisterSourceResponse], error)
	// Explicitly stop tracking a source. Deliberately not tied to any
	// consumer's lifecycle (e.g. a game server being deleted) -- content may
	// be worth keeping synced ("chambered") even with nothing currently
	// using it. Only removes this source_id's membership; a mod still
	// referenced by another source keeps syncing.
	DeregisterSource(context.Context, *connect.Request[v1.DeregisterSourceRequest]) (*connect.Response[v1.DeregisterSourceResponse], error)
	// Downloads server/CDLC depots plus every currently-registered
	// source's resolved mods into the shared golden content tree --
	// fire-and-forget (returns immediately, doesn't wait for the sync to
	// finish, which can range from instant (warm cache) to minutes
	// (cold)). No completion signal is returned at all -- the caller's
	// real interest is "make sure this eventually happens", not knowing
	// exactly when; GetSyncStats/ListSyncedMods reflect the result once
	// it's done. Every ArmaServer's own content comes from a read-only
	// btrfs snapshot of this tree instead (services/magpie-csi's
	// NodeStageVolume, mode: snapshot -- not this service's concern at
	// all anymore).
	SyncContent(context.Context, *connect.Request[v1.SyncContentRequest]) (*connect.Response[v1.SyncContentResponse], error)
	// Read a previously-registered source's current resolved mod list
	// without re-registering it (no candidate IDs needed, no Steam calls --
	// a plain read of what RegisterSource/the background poller last
	// resolved). Used by the controller's reconciler to build a server's
	// own -mod= args from the source IDs its spec references.
	GetSourceMods(context.Context, *connect.Request[v1.GetSourceModsRequest]) (*connect.Response[v1.GetSourceModsResponse], error)
	// Re-resolve a single already-registered source against its
	// originally-registered candidate IDs (stored from the last
	// RegisterSource call) -- the same work the background poller does
	// periodically for every source, triggered on demand for one source
	// instead of waiting for the next poll tick. Used by the controller's
	// UpdateServer/StartServer RPCs ("force resync this server's mod
	// sources") -- callers don't need to remember/resupply candidate IDs.
	RefreshSource(context.Context, *connect.Request[v1.RefreshSourceRequest]) (*connect.Response[v1.RefreshSourceResponse], error)
	// Every workshop mod ID currently tracked as verified-synced, and the
	// manifest_id it was last verified at. A plain read of the sync cache --
	// no Steam calls, no depot state (server/CDLC depots aren't included,
	// only workshop mods).
	ListSyncedMods(context.Context, *connect.Request[v1.ListSyncedModsRequest]) (*connect.Response[v1.ListSyncedModsResponse], error)
	// Clear one mod's "last verified" marker only -- never deletes its files
	// on disk. The next resolve pass genuinely re-verifies that mod's chunks
	// against Steam's current manifest, redownloading only whatever's
	// actually missing or divergent. Deliberately not a real
	// wipe-and-redownload -- this is the restricted, non-destructive shape
	// of "force refresh a mod's cache".
	InvalidateMod(context.Context, *connect.Request[v1.InvalidateModRequest]) (*connect.Response[v1.InvalidateModResponse], error)
	// A single mod's synced state plus every source_id currently
	// referencing it -- one extra query beyond ListSyncedMods, so kept
	// separate rather than having every list entry pay for it.
	GetSyncedMod(context.Context, *connect.Request[v1.GetSyncedModRequest]) (*connect.Response[v1.GetSyncedModResponse], error)
	// Cluster-wide totals: every synced mod's size deduplicated across
	// sources, plus base game/CDLC depot size -- for registry's
	// AdminService.GetDiskUsage.
	GetSyncStats(context.Context, *connect.Request[v1.GetSyncStatsRequest]) (*connect.Response[v1.GetSyncStatsResponse], error)
	// Establish (or replace) this cluster's Steam session from an
	// already-negotiated refresh token -- the interactive username+password
	// (+ Guard code) negotiation itself never happens inside this or any
	// other deployed service; it happens client-side, in
	// cli/magpie's `magpie admin refresh-steam-auth` (which runs the
	// Steam login flow client-side, then calls this RPC with just the
	// result). A password reaching this
	// process, even transiently, is exactly what this RPC shape is
	// designed to make impossible. On success, the new session is persisted
	// and this process exits to pick it up fresh on restart (see the
	// handler's own doc for why) -- the response still reaches the caller
	// first.
	RefreshSteamAuth(context.Context, *connect.Request[v1.RefreshSteamAuthRequest]) (*connect.Response[v1.RefreshSteamAuthResponse], error)
	// Whether the golden content tree is safe to snapshot from right now --
	// the controller polls this before creating an ArmaServer's Deployment
	// so a launcher Pod never gets a CSI snapshot of a base-game/CDLC sync
	// that's still mid-download (confirmed live: a server started against a
	// partial sync fails to even spawn arma3server_x64, "Permission denied",
	// since steamcmd doesn't set the binary's final mode/content until its
	// download actually completes). Deliberately not folded into
	// GetSyncStats -- that reports totals regardless of whether a sync is
	// currently touching them, this reports the one boolean the controller
	// actually needs to gate on.
	GetSyncStatus(context.Context, *connect.Request[v1.GetSyncStatusRequest]) (*connect.Response[v1.GetSyncStatusResponse], error)
}

// NewSyncServiceClient constructs a client for the sync.v1.SyncService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSyncServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SyncServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	syncServiceMethods := v1.File_sync_v1_sync_proto.Services().ByName("SyncService").Methods()
	return &syncServiceClient{
		registerSource: connect.NewClient[v1.RegisterSourceRequest, v1.RegisterSourceResponse](
			httpClient,
			baseURL+SyncServiceRegisterSourceProcedure,
			connect.WithSchema(syncServiceMethods.ByName("RegisterSource")),
			connect.WithClientOptions(opts...),
		),
		deregisterSource: connect.NewClient[v1.DeregisterSourceRequest, v1.DeregisterSourceResponse](
			httpClient,
			baseURL+SyncServiceDeregisterSourceProcedure,
			connect.WithSchema(syncServiceMethods.ByName("DeregisterSource")),
			connect.WithClientOptions(opts...),
		),
		syncContent: connect.NewClient[v1.SyncContentRequest, v1.SyncContentResponse](
			httpClient,
			baseURL+SyncServiceSyncContentProcedure,
			connect.WithSchema(syncServiceMethods.ByName("SyncContent")),
			connect.WithClientOptions(opts...),
		),
		getSourceMods: connect.NewClient[v1.GetSourceModsRequest, v1.GetSourceModsResponse](
			httpClient,
			baseURL+SyncServiceGetSourceModsProcedure,
			connect.WithSchema(syncServiceMethods.ByName("GetSourceMods")),
			connect.WithClientOptions(opts...),
		),
		refreshSource: connect.NewClient[v1.RefreshSourceRequest, v1.RefreshSourceResponse](
			httpClient,
			baseURL+SyncServiceRefreshSourceProcedure,
			connect.WithSchema(syncServiceMethods.ByName("RefreshSource")),
			connect.WithClientOptions(opts...),
		),
		listSyncedMods: connect.NewClient[v1.ListSyncedModsRequest, v1.ListSyncedModsResponse](
			httpClient,
			baseURL+SyncServiceListSyncedModsProcedure,
			connect.WithSchema(syncServiceMethods.ByName("ListSyncedMods")),
			connect.WithClientOptions(opts...),
		),
		invalidateMod: connect.NewClient[v1.InvalidateModRequest, v1.InvalidateModResponse](
			httpClient,
			baseURL+SyncServiceInvalidateModProcedure,
			connect.WithSchema(syncServiceMethods.ByName("InvalidateMod")),
			connect.WithClientOptions(opts...),
		),
		getSyncedMod: connect.NewClient[v1.GetSyncedModRequest, v1.GetSyncedModResponse](
			httpClient,
			baseURL+SyncServiceGetSyncedModProcedure,
			connect.WithSchema(syncServiceMethods.ByName("GetSyncedMod")),
			connect.WithClientOptions(opts...),
		),
		getSyncStats: connect.NewClient[v1.GetSyncStatsRequest, v1.GetSyncStatsResponse](
			httpClient,
			baseURL+SyncServiceGetSyncStatsProcedure,
			connect.WithSchema(syncServiceMethods.ByName("GetSyncStats")),
			connect.WithClientOptions(opts...),
		),
		refreshSteamAuth: connect.NewClient[v1.RefreshSteamAuthRequest, v1.RefreshSteamAuthResponse](
			httpClient,
			baseURL+SyncServiceRefreshSteamAuthProcedure,
			connect.WithSchema(syncServiceMethods.ByName("RefreshSteamAuth")),
			connect.WithClientOptions(opts...),
		),
		getSyncStatus: connect.NewClient[v1.GetSyncStatusRequest, v1.GetSyncStatusResponse](
			httpClient,
			baseURL+SyncServiceGetSyncStatusProcedure,
			connect.WithSchema(syncServiceMethods.ByName("GetSyncStatus")),
			connect.WithClientOptions(opts...),
		),
	}
}

// syncServiceClient implements SyncServiceClient.
type syncServiceClient struct {
	registerSource   *connect.Client[v1.RegisterSourceRequest, v1.RegisterSourceResponse]
	deregisterSource *connect.Client[v1.DeregisterSourceRequest, v1.DeregisterSourceResponse]
	syncContent      *connect.Client[v1.SyncContentRequest, v1.SyncContentResponse]
	getSourceMods    *connect.Client[v1.GetSourceModsRequest, v1.GetSourceModsResponse]
	refreshSource    *connect.Client[v1.RefreshSourceRequest, v1.RefreshSourceResponse]
	listSyncedMods   *connect.Client[v1.ListSyncedModsRequest, v1.ListSyncedModsResponse]
	invalidateMod    *connect.Client[v1.InvalidateModRequest, v1.InvalidateModResponse]
	getSyncedMod     *connect.Client[v1.GetSyncedModRequest, v1.GetSyncedModResponse]
	getSyncStats     *connect.Client[v1.GetSyncStatsRequest, v1.GetSyncStatsResponse]
	refreshSteamAuth *connect.Client[v1.RefreshSteamAuthRequest, v1.RefreshSteamAuthResponse]
	getSyncStatus    *connect.Client[v1.GetSyncStatusRequest, v1.GetSyncStatusResponse]
}

// RegisterSource calls sync.v1.SyncService.RegisterSource.
func (c *syncServiceClient) RegisterSource(ctx context.Context, req *connect.Request[v1.RegisterSourceRequest]) (*connect.Response[v1.RegisterSourceResponse], error) {
	return c.registerSource.CallUnary(ctx, req)
}

// DeregisterSource calls sync.v1.SyncService.DeregisterSource.
func (c *syncServiceClient) DeregisterSource(ctx context.Context, req *connect.Request[v1.DeregisterSourceRequest]) (*connect.Response[v1.DeregisterSourceResponse], error) {
	return c.deregisterSource.CallUnary(ctx, req)
}

// SyncContent calls sync.v1.SyncService.SyncContent.
func (c *syncServiceClient) SyncContent(ctx context.Context, req *connect.Request[v1.SyncContentRequest]) (*connect.Response[v1.SyncContentResponse], error) {
	return c.syncContent.CallUnary(ctx, req)
}

// GetSourceMods calls sync.v1.SyncService.GetSourceMods.
func (c *syncServiceClient) GetSourceMods(ctx context.Context, req *connect.Request[v1.GetSourceModsRequest]) (*connect.Response[v1.GetSourceModsResponse], error) {
	return c.getSourceMods.CallUnary(ctx, req)
}

// RefreshSource calls sync.v1.SyncService.RefreshSource.
func (c *syncServiceClient) RefreshSource(ctx context.Context, req *connect.Request[v1.RefreshSourceRequest]) (*connect.Response[v1.RefreshSourceResponse], error) {
	return c.refreshSource.CallUnary(ctx, req)
}

// ListSyncedMods calls sync.v1.SyncService.ListSyncedMods.
func (c *syncServiceClient) ListSyncedMods(ctx context.Context, req *connect.Request[v1.ListSyncedModsRequest]) (*connect.Response[v1.ListSyncedModsResponse], error) {
	return c.listSyncedMods.CallUnary(ctx, req)
}

// InvalidateMod calls sync.v1.SyncService.InvalidateMod.
func (c *syncServiceClient) InvalidateMod(ctx context.Context, req *connect.Request[v1.InvalidateModRequest]) (*connect.Response[v1.InvalidateModResponse], error) {
	return c.invalidateMod.CallUnary(ctx, req)
}

// GetSyncedMod calls sync.v1.SyncService.GetSyncedMod.
func (c *syncServiceClient) GetSyncedMod(ctx context.Context, req *connect.Request[v1.GetSyncedModRequest]) (*connect.Response[v1.GetSyncedModResponse], error) {
	return c.getSyncedMod.CallUnary(ctx, req)
}

// GetSyncStats calls sync.v1.SyncService.GetSyncStats.
func (c *syncServiceClient) GetSyncStats(ctx context.Context, req *connect.Request[v1.GetSyncStatsRequest]) (*connect.Response[v1.GetSyncStatsResponse], error) {
	return c.getSyncStats.CallUnary(ctx, req)
}

// RefreshSteamAuth calls sync.v1.SyncService.RefreshSteamAuth.
func (c *syncServiceClient) RefreshSteamAuth(ctx context.Context, req *connect.Request[v1.RefreshSteamAuthRequest]) (*connect.Response[v1.RefreshSteamAuthResponse], error) {
	return c.refreshSteamAuth.CallUnary(ctx, req)
}

// GetSyncStatus calls sync.v1.SyncService.GetSyncStatus.
func (c *syncServiceClient) GetSyncStatus(ctx context.Context, req *connect.Request[v1.GetSyncStatusRequest]) (*connect.Response[v1.GetSyncStatusResponse], error) {
	return c.getSyncStatus.CallUnary(ctx, req)
}

// SyncServiceHandler is an implementation of the sync.v1.SyncService service.
type SyncServiceHandler interface {
	// Resolve `candidate_ids` (each may be an individual mod or a collection
	// -- the caller doesn't need to know which) and register them as a named
	// source in the persistent sync registry. Registering an existing
	// source_id again refreshes its membership (diffed against what was
	// previously registered) rather than erroring -- this is also how a
	// source is first created, there is no separate "create" call.
	RegisterSource(context.Context, *connect.Request[v1.RegisterSourceRequest]) (*connect.Response[v1.RegisterSourceResponse], error)
	// Explicitly stop tracking a source. Deliberately not tied to any
	// consumer's lifecycle (e.g. a game server being deleted) -- content may
	// be worth keeping synced ("chambered") even with nothing currently
	// using it. Only removes this source_id's membership; a mod still
	// referenced by another source keeps syncing.
	DeregisterSource(context.Context, *connect.Request[v1.DeregisterSourceRequest]) (*connect.Response[v1.DeregisterSourceResponse], error)
	// Downloads server/CDLC depots plus every currently-registered
	// source's resolved mods into the shared golden content tree --
	// fire-and-forget (returns immediately, doesn't wait for the sync to
	// finish, which can range from instant (warm cache) to minutes
	// (cold)). No completion signal is returned at all -- the caller's
	// real interest is "make sure this eventually happens", not knowing
	// exactly when; GetSyncStats/ListSyncedMods reflect the result once
	// it's done. Every ArmaServer's own content comes from a read-only
	// btrfs snapshot of this tree instead (services/magpie-csi's
	// NodeStageVolume, mode: snapshot -- not this service's concern at
	// all anymore).
	SyncContent(context.Context, *connect.Request[v1.SyncContentRequest]) (*connect.Response[v1.SyncContentResponse], error)
	// Read a previously-registered source's current resolved mod list
	// without re-registering it (no candidate IDs needed, no Steam calls --
	// a plain read of what RegisterSource/the background poller last
	// resolved). Used by the controller's reconciler to build a server's
	// own -mod= args from the source IDs its spec references.
	GetSourceMods(context.Context, *connect.Request[v1.GetSourceModsRequest]) (*connect.Response[v1.GetSourceModsResponse], error)
	// Re-resolve a single already-registered source against its
	// originally-registered candidate IDs (stored from the last
	// RegisterSource call) -- the same work the background poller does
	// periodically for every source, triggered on demand for one source
	// instead of waiting for the next poll tick. Used by the controller's
	// UpdateServer/StartServer RPCs ("force resync this server's mod
	// sources") -- callers don't need to remember/resupply candidate IDs.
	RefreshSource(context.Context, *connect.Request[v1.RefreshSourceRequest]) (*connect.Response[v1.RefreshSourceResponse], error)
	// Every workshop mod ID currently tracked as verified-synced, and the
	// manifest_id it was last verified at. A plain read of the sync cache --
	// no Steam calls, no depot state (server/CDLC depots aren't included,
	// only workshop mods).
	ListSyncedMods(context.Context, *connect.Request[v1.ListSyncedModsRequest]) (*connect.Response[v1.ListSyncedModsResponse], error)
	// Clear one mod's "last verified" marker only -- never deletes its files
	// on disk. The next resolve pass genuinely re-verifies that mod's chunks
	// against Steam's current manifest, redownloading only whatever's
	// actually missing or divergent. Deliberately not a real
	// wipe-and-redownload -- this is the restricted, non-destructive shape
	// of "force refresh a mod's cache".
	InvalidateMod(context.Context, *connect.Request[v1.InvalidateModRequest]) (*connect.Response[v1.InvalidateModResponse], error)
	// A single mod's synced state plus every source_id currently
	// referencing it -- one extra query beyond ListSyncedMods, so kept
	// separate rather than having every list entry pay for it.
	GetSyncedMod(context.Context, *connect.Request[v1.GetSyncedModRequest]) (*connect.Response[v1.GetSyncedModResponse], error)
	// Cluster-wide totals: every synced mod's size deduplicated across
	// sources, plus base game/CDLC depot size -- for registry's
	// AdminService.GetDiskUsage.
	GetSyncStats(context.Context, *connect.Request[v1.GetSyncStatsRequest]) (*connect.Response[v1.GetSyncStatsResponse], error)
	// Establish (or replace) this cluster's Steam session from an
	// already-negotiated refresh token -- the interactive username+password
	// (+ Guard code) negotiation itself never happens inside this or any
	// other deployed service; it happens client-side, in
	// cli/magpie's `magpie admin refresh-steam-auth` (which runs the
	// Steam login flow client-side, then calls this RPC with just the
	// result). A password reaching this
	// process, even transiently, is exactly what this RPC shape is
	// designed to make impossible. On success, the new session is persisted
	// and this process exits to pick it up fresh on restart (see the
	// handler's own doc for why) -- the response still reaches the caller
	// first.
	RefreshSteamAuth(context.Context, *connect.Request[v1.RefreshSteamAuthRequest]) (*connect.Response[v1.RefreshSteamAuthResponse], error)
	// Whether the golden content tree is safe to snapshot from right now --
	// the controller polls this before creating an ArmaServer's Deployment
	// so a launcher Pod never gets a CSI snapshot of a base-game/CDLC sync
	// that's still mid-download (confirmed live: a server started against a
	// partial sync fails to even spawn arma3server_x64, "Permission denied",
	// since steamcmd doesn't set the binary's final mode/content until its
	// download actually completes). Deliberately not folded into
	// GetSyncStats -- that reports totals regardless of whether a sync is
	// currently touching them, this reports the one boolean the controller
	// actually needs to gate on.
	GetSyncStatus(context.Context, *connect.Request[v1.GetSyncStatusRequest]) (*connect.Response[v1.GetSyncStatusResponse], error)
}

// NewSyncServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSyncServiceHandler(svc SyncServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	syncServiceMethods := v1.File_sync_v1_sync_proto.Services().ByName("SyncService").Methods()
	syncServiceRegisterSourceHandler := connect.NewUnaryHandler(
		SyncServiceRegisterSourceProcedure,
		svc.RegisterSource,
		connect.WithSchema(syncServiceMethods.ByName("RegisterSource")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceDeregisterSourceHandler := connect.NewUnaryHandler(
		SyncServiceDeregisterSourceProcedure,
		svc.DeregisterSource,
		connect.WithSchema(syncServiceMethods.ByName("DeregisterSource")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceSyncContentHandler := connect.NewUnaryHandler(
		SyncServiceSyncContentProcedure,
		svc.SyncContent,
		connect.WithSchema(syncServiceMethods.ByName("SyncContent")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceGetSourceModsHandler := connect.NewUnaryHandler(
		SyncServiceGetSourceModsProcedure,
		svc.GetSourceMods,
		connect.WithSchema(syncServiceMethods.ByName("GetSourceMods")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceRefreshSourceHandler := connect.NewUnaryHandler(
		SyncServiceRefreshSourceProcedure,
		svc.RefreshSource,
		connect.WithSchema(syncServiceMethods.ByName("RefreshSource")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceListSyncedModsHandler := connect.NewUnaryHandler(
		SyncServiceListSyncedModsProcedure,
		svc.ListSyncedMods,
		connect.WithSchema(syncServiceMethods.ByName("ListSyncedMods")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceInvalidateModHandler := connect.NewUnaryHandler(
		SyncServiceInvalidateModProcedure,
		svc.InvalidateMod,
		connect.WithSchema(syncServiceMethods.ByName("InvalidateMod")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceGetSyncedModHandler := connect.NewUnaryHandler(
		SyncServiceGetSyncedModProcedure,
		svc.GetSyncedMod,
		connect.WithSchema(syncServiceMethods.ByName("GetSyncedMod")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceGetSyncStatsHandler := connect.NewUnaryHandler(
		SyncServiceGetSyncStatsProcedure,
		svc.GetSyncStats,
		connect.WithSchema(syncServiceMethods.ByName("GetSyncStats")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceRefreshSteamAuthHandler := connect.NewUnaryHandler(
		SyncServiceRefreshSteamAuthProcedure,
		svc.RefreshSteamAuth,
		connect.WithSchema(syncServiceMethods.ByName("RefreshSteamAuth")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceGetSyncStatusHandler := connect.NewUnaryHandler(
		SyncServiceGetSyncStatusProcedure,
		svc.GetSyncStatus,
		connect.WithSchema(syncServiceMethods.ByName("GetSyncStatus")),
		connect.WithHandlerOptions(opts...),
	)
	return "/sync.v1.SyncService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SyncServiceRegisterSourceProcedure:
			syncServiceRegisterSourceHandler.ServeHTTP(w, r)
		case SyncServiceDeregisterSourceProcedure:
			syncServiceDeregisterSourceHandler.ServeHTTP(w, r)
		case SyncServiceSyncContentProcedure:
			syncServiceSyncContentHandler.ServeHTTP(w, r)
		case SyncServiceGetSourceModsProcedure:
			syncServiceGetSourceModsHandler.ServeHTTP(w, r)
		case SyncServiceRefreshSourceProcedure:
			syncServiceRefreshSourceHandler.ServeHTTP(w, r)
		case SyncServiceListSyncedModsProcedure:
			syncServiceListSyncedModsHandler.ServeHTTP(w, r)
		case SyncServiceInvalidateModProcedure:
			syncServiceInvalidateModHandler.ServeHTTP(w, r)
		case SyncServiceGetSyncedModProcedure:
			syncServiceGetSyncedModHandler.ServeHTTP(w, r)
		case SyncServiceGetSyncStatsProcedure:
			syncServiceGetSyncStatsHandler.ServeHTTP(w, r)
		case SyncServiceRefreshSteamAuthProcedure:
			syncServiceRefreshSteamAuthHandler.ServeHTTP(w, r)
		case SyncServiceGetSyncStatusProcedure:
			syncServiceGetSyncStatusHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSyncServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSyncServiceHandler struct{}

func (UnimplementedSyncServiceHandler) RegisterSource(context.Context, *connect.Request[v1.RegisterSourceRequest]) (*connect.Response[v1.RegisterSourceResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sync.v1.SyncService.RegisterSource is not implemented"))
}

func (UnimplementedSyncServiceHandler) DeregisterSource(context.Context, *connect.Request[v1.DeregisterSourceRequest]) (*connect.Response[v1.DeregisterSourceResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sync.v1.SyncService.DeregisterSource is not implemented"))
}

func (UnimplementedSyncServiceHandler) SyncContent(context.Context, *connect.Request[v1.SyncContentRequest]) (*connect.Response[v1.SyncContentResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sync.v1.SyncService.SyncContent is not implemented"))
}

func (UnimplementedSyncServiceHandler) GetSourceMods(context.Context, *connect.Request[v1.GetSourceModsRequest]) (*connect.Response[v1.GetSourceModsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sync.v1.SyncService.GetSourceMods is not implemented"))
}

func (UnimplementedSyncServiceHandler) RefreshSource(context.Context, *connect.Request[v1.RefreshSourceRequest]) (*connect.Response[v1.RefreshSourceResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sync.v1.SyncService.RefreshSource is not implemented"))
}

func (UnimplementedSyncServiceHandler) ListSyncedMods(context.Context, *connect.Request[v1.ListSyncedModsRequest]) (*connect.Response[v1.ListSyncedModsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sync.v1.SyncService.ListSyncedMods is not implemented"))
}

func (UnimplementedSyncServiceHandler) InvalidateMod(context.Context, *connect.Request[v1.InvalidateModRequest]) (*connect.Response[v1.InvalidateModResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sync.v1.SyncService.InvalidateMod is not implemented"))
}

func (UnimplementedSyncServiceHandler) GetSyncedMod(context.Context, *connect.Request[v1.GetSyncedModRequest]) (*connect.Response[v1.GetSyncedModResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sync.v1.SyncService.GetSyncedMod is not implemented"))
}

func (UnimplementedSyncServiceHandler) GetSyncStats(context.Context, *connect.Request[v1.GetSyncStatsRequest]) (*connect.Response[v1.GetSyncStatsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sync.v1.SyncService.GetSyncStats is not implemented"))
}

func (UnimplementedSyncServiceHandler) RefreshSteamAuth(context.Context, *connect.Request[v1.RefreshSteamAuthRequest]) (*connect.Response[v1.RefreshSteamAuthResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sync.v1.SyncService.RefreshSteamAuth is not implemented"))
}

func (UnimplementedSyncServiceHandler) GetSyncStatus(context.Context, *connect.Request[v1.GetSyncStatusRequest]) (*connect.Response[v1.GetSyncStatusResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sync.v1.SyncService.GetSyncStatus is not implemented"))
}
