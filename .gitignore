/target
node_modules/
# No leading slash -- every service stages its pre-built binary into its
# own dist/ (services/*/dist/, packaged by that service's Dockerfile),
# not just one at the repo root.
dist/
